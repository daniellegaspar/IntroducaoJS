// Código para dizer se o pet está com o peso ideal //

var peso = 3.0 // colocar peso do animal //
    if (peso <4.0) {
        console.log("Abaixo do peso.")
    } else if (peso >10.0) {
        console.log("Acima do peso")
    } else {
    console.log("Peso normal.")
    }